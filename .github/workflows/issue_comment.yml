name: create a comment on new issue

on:
  issues:
    types: [opened]

jobs:
  comment:
    runs-on:
      - name: "dump github context"
        run: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
